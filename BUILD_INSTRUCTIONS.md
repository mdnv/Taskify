# 📱 Сборка Taskify для Android APK и iOS

## Шаг 1: Сборка Android APK

```bash
npx eas build --platform android --release
```

**Что произойдет:**
1. EAS загрузит ваш проект на сервер
2. Скомпилирует приложение для Android
3. Создаст подписанный APK файл
4. Вернет вам ссылку на скачивание

**Времени займет:** 5-10 минут

**Результат:**
```
✓ Build successful!
📥 Download: https://eas-builds.expo.dev/...apk
```

### Как установить APK на Android:

**Вариант 1: Через EAS**
1. Скопируй ссылку из консоли
2. Отправь ссылку на телефон через WhatsApp/Telegram/Email
3. На телефоне нажми на ссылку
4. APK скачается и установится

**Вариант 2: Локально (если нужно тестировать сразу)**
```bash
# Сначала надо получить APK локально (долгий способ)
# Лучше использовать вариант 1
```

**Вариант 3: Через Android Studio (если установлена)**
```bash
# 1. Скачай APK с ссылки
# 2. Открой Android Studio
# 3. Device Manager → Run on Device
# 4. Выбери APK файл
```

---

## Шаг 2: Тестирование на Android

После установки APK:
1. ✅ Проверь все функции работают
2. ✅ Проверь виджет на главном экране
   - Долгое нажатие → Виджеты → Taskify → добавить
3. ✅ Проверь уведомления
4. ✅ Проверь все экраны (Tasks, Categories, Analytics)

---

## Шаг 3: Сборка iOS

После тестирования Android:

```bash
npx eas build --platform ios --release
```

**Что произойдет:**
1. EAS скомпилирует для iOS
2. Создаст .ipa файл (iOS app package)
3. Подпишет сертификатом (нужен Apple Developer Account)

**Времени займет:** 10-15 минут

**⚠️ ВАЖНО ДЛЯ iOS:**
Нужен Apple Developer Account:
- https://developer.apple.com
- Стоит ~99$/год

---

## Альтернатива: Сборка обоих сразу

Если хочешь сразу оба:

```bash
npx eas build --platform all --release
```

**Плюсы:**
- Одна очередь
- Параллельная сборка
- Одна команда

**Минусы:**
- Дольше ждать оба файла
- Сложнее отследить ошибки если они есть

---

## 🎯 РЕКОМЕНДУЕМЫЙ ПОРЯДОК:

### День 1: Тестирование Android
```bash
# 1. Сборка Android
npx eas build --platform android --release

# 2. Скачай APK и установи на телефон
# 3. Тестируй 2-3 часа

# 4. Если все работает → идем к шагу 2
```

### День 2: Сборка iOS (если нужна)
```bash
# Когда уверен что Android работает:
npx eas build --platform ios --release

# Нужен Mac для установки на iPhone
# Или используй TestFlight для удаленного тестирования
```

---

## 📊 Сравнение: APK vs IPA

| Параметр | APK (Android) | IPA (iOS) |
|----------|---------------|----------|
| **Что это** | .apk файл | .ipa файл |
| **Где установить** | Android телефон | iPhone |
| **Как установить** | Скачать и открыть | TestFlight или Xcode |
| **Account нужен** | Нет (бесплатно) | Да (~99$/год) |
| **Время сборки** | 5-10 мин | 10-15 мин |
| **Подпись** | Автоматическая | Apple Certificate |

---

## 🔧 Команды для каждой платформы

### Только Android APK
```bash
npx eas build --platform android --release
```

### Только iOS IPA
```bash
npx eas build --platform ios --release
```

### Оба файла
```bash
npx eas build --platform all --release
```

### Android Debug (для тестирования)
```bash
npx eas build --platform android
# Без --release флага, быстрее собирается
```

### iOS Simulator (для Mac)
```bash
npx eas build --platform ios --profile preview
```

---

## 📍 Отслеживание сборки

После запуска команды, можешь посмотреть статус:

```bash
# Показать последние сборки
npx eas build:list

# Просмотр конкретной сборки
npx eas build:view <build-id>
```

---

## ✅ Чек-лист перед сборкой

Перед запуском команды проверь:

- [ ] Все файлы закоммичены: `git status`
- [ ] Версия обновлена: `app.json` версия актуальна
- [ ] Нет TypeScript ошибок: `npx tsc --noEmit`
- [ ] node_modules актуальны: `npm ci` (если нужно)
- [ ] EAS CLI установлен: `npx eas --version`
- [ ] Логин в EAS: `npx eas login`

---

## 🚀 ИТОГОВАЯ КОМАНДА

Скопируй и запусти:

```bash
npx eas build --platform android --release
```

Это запустит сборку APK для Android.

После тестирования:

```bash
npx eas build --platform ios --release
```

Это запустит сборку для iOS.

---

## 💡 Советы

1. **Сначала тестируй Android** - быстрее, дешевле
2. **Смотри логи в консоли** - там видны все ошибки
3. **Если ошибка** - сразу видишь где и можешь исправить
4. **Сохранять версию между сборками** - меняй только patch версию

---

## 📞 Если что-то не работает

```bash
# 1. Проверь статус сборки
npx eas build:list

# 2. Посмотри логи
npx eas build:view <build-id> --log

# 3. Перезапусти без кэша
eas build --platform android --release --clear-cache
```

---

**Готово! Теперь запусти сборку Android APK 🚀**
