# 🚀 Quick Start Guide - Taskify Widgets & Animations

## Быстрый старт за 5 минут

### 1️⃣ Запустить приложение

```bash
cd Taskify
npm start
# или
expo start
```

### 2️⃣ Увидеть виджет на главном экране

- Откройте HomeScreen
- В верхней части вы увидите **HomeWidget** с:
  - 📊 Статистикой (Всего, Выполнено, Ожидает)
  - 📈 Прогресс-баром
  - 📝 Предпросмотром задач
  - ✅ Кнопкой быстрого выполнения

### 3️⃣ Попробовать анимации

Нажимайте на элементы виджета - вы увидите:
- 🎭 Scale эффект при нажатии
- 🌀 Bounce анимацию
- ✨ Smooth transitions

### 4️⃣ Добавить задачу и увидеть обновление

1. Нажмите "Add a quick task..."
2. Введите название
3. Нажмите кнопку +
4. **Виджет обновится автоматически** 🔄

### 5️⃣ Отметить задачу как выполненную

- Нажмите на задачу в виджете
- Она будет отмечена ✅
- Статистика обновится

## 🎨 Попробовать все анимации

1. Создайте тестовый компонент в любом экране:

```tsx
import { AnimationsDemo } from '@/components/animations/AnimationsDemo';

export const TestScreen = () => {
  return <AnimationsDemo />;
};
```

2. Откройте экран и посмотрите:
   - Fade эффекты
   - Pulse анимации
   - Scale трансформации
   - Slide переходы

## 📱 Использовать в своем компоненте

```tsx
import { AnimatedContainer } from '@/components/animations/AnimatedComponents';
import { HomeWidget } from '@/widgets';

export const MyScreen = () => {
  return (
    <AnimatedContainer type="slideUp" delay={100}>
      <HomeWidget
        tasks={tasks}
        onToggleTask={(id) => toggleTask(id)}
        onOpenApp={() => {}}
      />
    </AnimatedContainer>
  );
};
```

## 🎯 Основные компоненты

### HomeWidget
```tsx
<HomeWidget 
  tasks={tasks}                           // Array of tasks
  onToggleTask={(taskId) => {}}          // Callback when task toggled
  onOpenApp={() => {}}                   // Callback to open app
/>
```

### AnimatedContainer
```tsx
<AnimatedContainer 
  type="slideUp"    // 'fadeIn' | 'slideUp' | 'scaleIn' | 'none'
  delay={100}       // Delay in ms
  duration={400}    // Duration in ms
>
  <Content />
</AnimatedContainer>
```

### BouncyButton
```tsx
<BouncyButton onPress={() => {}}>
  <ButtonContent />
</BouncyButton>
```

## 🎨 Цвета для использования

```tsx
// Primary - для основных действий
backgroundColor: '#6366F1'

// Success - для завершенных задач
backgroundColor: '#10B981'

// Warning - для среднего приоритета
backgroundColor: '#F59E0B'

// Error - для высокого приоритета
backgroundColor: '#EF4444'

// Text colors
color: '#1E293B'       // Основной текст
color: '#94A3B8'       // Вспомогательный текст
```

## ⚡ Советы производительности

✅ Используйте `useNativeDriver: true` (уже установлено)
✅ Ограничивайте количество одновременных анимаций
✅ Используйте `shouldRasterizeIOS` для комплексных компонентов
✅ Кешируйте стили при возможности

## 🐛 Решение проблем

**Анимация не работает?**
```tsx
// Убедитесь, что используете Animated компоненты:
import { Animated } from 'react-native';
```

**Виджет не обновляется?**
```tsx
// Проверьте, что updateWidget вызывается:
import { widgetService } from '@/utils/widgetService';
await widgetService.updateWidget(tasks);
```

**Типы не работают?**
```bash
# Переустановите зависимости:
npm install
# или
yarn install
```

## 📚 Дополнительные материалы

- `ANIMATIONS_README.md` - полное руководство
- `WIDGET_GUIDE.md` - описание виджет системы
- `IMPLEMENTATION_COMPLETE.md` - что было реализовано
- `src/components/animations/AnimationsDemo.tsx` - интерактивная демонстрация

## ✅ Чек-лист

- [ ] Запустил приложение
- [ ] Увидел виджет на главном экране
- [ ] Добавил задачу и виджет обновился
- [ ] Отметил задачу как выполненную
- [ ] Попробовал нажимать на элементы (видны анимации)
- [ ] Проверил демонстрацию анимаций
- [ ] Все работает без ошибок ✅

## 🎉 Готово!

Теперь у вас есть:
- ✨ Красивые анимации повсеместно
- 📱 Виджет для главного экрана
- 🎯 Real-time синхронизация данных
- ⚡ Оптимизированная производительность
- 🔐 Полная Type Safety

Наслаждайтесь разработкой! 🚀

---

Вопросы? Посмотрите документацию в корневой папке проекта.
